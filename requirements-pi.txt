# Pi-only runtime deps (real GPIO/SPI + panel driver). Human-edited INPUT only —
# the device never installs from this file directly.
#
# scripts/deploy.sh and docs/runbook.md §5 install from requirements.lock with
# `pip install --require-hashes` (SECURITY.md §6, issue #68). After editing this
# file, regenerate the lock:
#   uv pip compile --universal --generate-hashes --no-header \
#       requirements-pi.txt -o requirements.lock
#
# lgpio and spidev have no cp313 wheel and build from their (hash-verified)
# sdists on the Pi; scripts/install.sh apt-installs the toolchain first —
# swig + Python headers (issue #66) and liblgpio-dev, which the lgpio C
# extension links against (issue #85).
-r requirements-base.txt
inky
gpiozero
lgpio
spidev
